{
  "name": "@renft/subgraph",
  "license": "GPL-3.0-or-later",
  "version": "1.0.0",
  "author": "ReNFT Team",
  "scripts": {
    "codegen": "yarn graph codegen",
    "build": "yarn graph build",
    "deploy:ropsten": "yarn graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ re-nft/renft-ropsten --access-token",
    "create:local": "yarn graph create --node http://localhost:8020 re-nft/renft-local",
    "deploy:local": "yarn graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 re-nft/renft-local",
    "remove:local": "yarn graph remove --node http://localhost:8020 re-nft/renft-local"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.19.0",
    "@graphprotocol/graph-ts": "^0.19.0"
  }
}
